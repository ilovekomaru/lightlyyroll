DISCORD_TOKEN=
# Optional: a test server ID for instant slash-command sync during development.
GUILD_ID=
# Discord user id allowed to run owner-only commands (Developer Mode -> Copy User ID).
OWNER_ID=
